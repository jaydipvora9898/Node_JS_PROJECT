<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= movie.name %> - PVR Cinemas</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

  <style>
    :root {
      --pvr-dark: #ffffff;
      --pvr-red: #e50914;
      --pvr-gold: #d4af37;
      --pvr-gray: #f4f4f4;
      --pvr-light: #333333;
      --pvr-view: #0b6470;
      --card-bg: #ffffff;
      --body-bg: #f8f9fa;
      --shadow-color: rgba(0, 0, 0, 0.1);
    }

    body {
      background: var(--body-bg);
      font-family: 'Montserrat', sans-serif;
      color: var(--pvr-light);
      min-height: 100vh;
    }

    .pvr-navbar {
      background-color: #ffffff; 
      padding: 0.6rem 1rem;
      box-shadow: 0 2px 10px var(--shadow-color);
    }
    .pvr-logo {
      color: var(--pvr-gold) !important;
      font-weight: bold;
      font-size: 1.5rem;
    }
    .nav-link {
      color: #333 !important;
    }
    .nav-link:hover {
      color: var(--pvr-gold) !important;
    }

    .movie-detail-container {
      max-width: 1100px;
      margin: 3rem auto;
      display: grid;
      grid-template-columns: 1fr 1.5fr;
      gap: 3rem;
      background: var(--card-bg);
      border-radius: 20px;
      overflow: hidden;
      border: none;
      box-shadow: 0 20px 50px var(--shadow-color);
    }

    .movie-poster {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
    }

    .movie-info {
      padding: 3rem 2rem;
    }
    .movie-title {
      font-family: 'Oswald', sans-serif;
      font-size: 2.5rem;
      color: #222;
      margin-bottom: 1rem;
      position: relative;
      font-weight: 700;
    }
    
    .movie-desc {
      font-size: 1.1rem;
      color: #555;
      margin: 1.5rem 0 2rem;
      line-height: 1.8;
    }

    .movie-meta {
      display: flex;
      justify-content: space-between;
      background: #f8f9fa;
      padding: 1.5rem;
      border-radius: 16px;
      margin-bottom: 2rem;
      box-shadow: inset 0 2px 5px rgba(0,0,0,0.05);
    }
    .meta-item {
      text-align: center;
      flex: 1;
    }
    .meta-label {
      font-size: 0.8rem;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: #888;
      font-weight: 600;
      margin-bottom: 5px;
    }
    .meta-value {
      font-weight: 700;
      color: #333;
      font-size: 1.1rem;
    }

    .movie-age {
      display: inline-block;
      background: rgba(229, 9, 20, 0.1);
      color: var(--pvr-red);
      padding: 8px 20px;
      border-radius: 50px;
      font-weight: 700;
      border: 1px solid rgba(229, 9, 20, 0.2);
      margin-bottom: 1rem;
      margin-top: .5rem;
    }

    .movie-actions {
      display: flex;
      gap: 1.5rem;
      margin-top: 2rem;
    }
    .btn-pvr {
      flex: 1;
      padding: 1rem;
      border-radius: 12px;
      font-weight: 700;
      text-transform: uppercase;
      font-size: 1rem;
      border: none;
      transition: all 0.3s ease;
      cursor: pointer;
    }
    .btn-book {
      background: var(--pvr-gold);
      color: #fff;
      box-shadow: 0 4px 15px rgba(212, 175, 55, 0.3);
    }
    .btn-book:hover {
      background: #c5a028;
      transform: translateY(-3px);
      box-shadow: 0 8px 20px rgba(212, 175, 55, 0.4);
    }
    .btn-back {
      background: white;
      color: #555;
      border: 2px solid #eee;
    }
    .btn-back:hover {
      background: #f8f9fa;
      border-color: #ddd;
      color: #333;
      transform: translateY(-3px);
    }

    @media (max-width: 768px) {
      .movie-detail-container {
        grid-template-columns: 1fr;
        margin: 1rem;
      }
    }

    .pvr-footer {
      text-align: center;
      margin-top: 3rem;
      padding: 1.5rem;
      border-top: 1px solid #eee;
      color: #777;
    }
  </style>
</head>
<body>

  <nav class="navbar navbar-expand-lg pvr-navbar">
    <div class="container-fluid">
      <a class="navbar-brand pvr-logo" href="/">🎬 PVR Cinemas</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarPVR">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarPVR">
        <ul class="navbar-nav me-auto">
          <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="movie-detail-container">
    <div>
      <img src="<%= movie.image %>" alt="<%= movie.name %>" class="movie-poster">
    </div>
    <div class="movie-info">
      <h2 class="movie-title"><%= movie.name %></h2>
      <div class="movie-age">Age: <%= movie.ageValidation %></div>
      <p class="movie-desc"><%= movie.desc %></p>

      <div class="movie-meta">
        <div class="meta-item">
          <div class="meta-label">Status</div>
          <div class="meta-value">₹<%= movie.status %></div>
        </div>
        <div class="meta-item">
          <div class="meta-label">Language</div>
          <div class="meta-value"><%= movie.language %></div>
        </div>
        <div class="meta-item">
          <div class="meta-label">Category</div>
          <div class="meta-value"><%= movie.category %></div>
        </div>
        <div class="meta-item">
          <div class="meta-label">Rating</div>
          <div class="meta-value"><i class="fas fa-star"></i> <%= movie.rating %>/10</div>
        </div>
      </div>

      <!-- Second Row (Release Date only) -->
      <div class="movie-meta">
        <div class="meta-item" style="flex: 1; text-align: left;">
          <div class="meta-label">Movie Timing</div>
          <div class="meta-value">
            <i class="fas fa-calendar-alt"></i>
            <%= movie.duration %>
          </div>
        </div>
      </div>

      <div class="movie-actions">
        <a href="/" class="btn-pvr btn-book">Book Now</a>
        <a href="/" class="btn-pvr btn-back">Back to Movies</a>
      </div>
    </div>
  </div>

  <div class="pvr-footer">
    <p>© 2023 PVR Cinemas Premium Experience. All rights reserved.</p>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
